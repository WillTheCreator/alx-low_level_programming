#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)

{
char alpha;                                                                                                                           
char new = '\n';             
for (alpha = 'a'; alpha <= 'z' ; ++alpha)                                                                                             
{
if (alpha = 'p' and alpha = 'e')
{
continue;
}
putchar(alpha);  
}                                                                                                                                     
putchar(new);
return (0);
}
