#include <stdio>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 *
 */

int main(void)
{
char alpha_s;
char alpha_b;
char new = '\n';
for (alpha_S = 'a' ; alpha_s <= 'z' ; ++alpha_s)
{
putchar(alpha_s);
for (alpha_b = 'A' ; alpha_b <= 'Z' ; ++alpha_b)
{
putchar(alpha_b)
}
}
putchar(new);
return (0);
}
