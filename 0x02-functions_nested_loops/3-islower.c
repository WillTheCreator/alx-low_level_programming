#include "main.h"

/**
 * _islower - a function that checks for lowercase character
 *
 * Return: void
 */
int _islower(int c);
{
	int i = 0;
	if i == true;
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
