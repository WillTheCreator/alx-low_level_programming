#include "main.h"

/**
 * _islower - a function that checks for lowercase character
 *
 * Return: 1 if successfull || 0 if otherwise
 */
int _islower(int c)
{
	int i = 0;
	for (i in c)
	{
		if (_islower(int c))
		{
			return (1);
		}
		else
		{
			return (0);
		}
	}
}
