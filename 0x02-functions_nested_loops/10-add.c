#include "main.h"

/**
 * add - function that adds two integers and returns the result
 * @c: integer variable storing a digit
 * @d: integer variable storing a digit
 * Return: addition of the two numbers
 */
int add(int c, int d)
{
	return (c + d);
}


